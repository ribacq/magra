#include "word.h"

// Word::Word is the constructor for the Word class.
Word::Word(std::string text, std::string meaning, std::string description): text(text), meaning(meaning), description(description) {}
